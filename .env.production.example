# Production Environment Variables for Google Cloud Run
# These should be set as environment variables in Google Cloud Run

# Application Environment
NODE_ENV=production
PORT=3000

# Database Configuration
DB_HOST=your-cloud-sql-instance-ip
DB_PORT=3306
DB_USERNAME=karaoke
DB_PASSWORD=GC(*g""\9SH@{vBr
DB_DATABASE=karaoke

# Optional: If using Cloud SQL Proxy or Unix sockets
# DB_SOCKET_PATH=/cloudsql/your-project:region:instance-name
